machine:
  node:
    version: 5.1.0

test:
  post:
    - bundle exec jekyll build
    - bundle exec htmlproofer ./_site --check-opengraph true --http-status-ignore "408,301" --url-ignore "/patrickmarsceill.com/" --empty-alt-ignore true
