machine:
  node:
    version: 6.9.5

test:
  post:
    - npm run build
    - bundle exec htmlproofer ./dist --check-opengraph true --check-html true --http-status-ignore '408,301,403,0,530' --url-ignore '/patrickmarsceill.com/,https://indieauth.com/auth,https://pmarsceill-micropub.now.sh/' --empty-alt-ignore true
